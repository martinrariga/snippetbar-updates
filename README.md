# Snippetbar updates

This repository serves the [Sparkle](https://sparkle-project.org) update feed for
Snippetbar.

- `appcast.xml` — the feed the app checks, served over GitHub Pages
- Release archives are attached as assets to the `updates` release

Both are published by `scripts/release.sh` in the Snippetbar source repository.
Nothing here is edited by hand.
